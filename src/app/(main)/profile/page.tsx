import { Header } from "@/widgets/headers";
import { Profile } from "@/widgets/profile";

const ProfilePage = () => {
  return (
    <div>
      <Header />
      <Profile />
    </div>
  );
};

export default ProfilePage;
