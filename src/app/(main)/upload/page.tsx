import { Header } from "@/widgets/headers";
import { UploadBuild } from "@/widgets/upload-build";

const UploadPage = () => {
  return (
    <div>
      <Header />
      <UploadBuild />
    </div>
  );
};

export default UploadPage;
